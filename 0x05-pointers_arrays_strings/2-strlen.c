#include "main.h"
/**
 *_strlen - returns the length of a string
 * @s: string
 *Return: returns lenght;
 */
int _strlen(char *s)
{
int sum, incrementation;

incrementation = 0;
for (sum = 0; s[sum] != '\0'; sum++)
incrementation++;

return (inc);
}
